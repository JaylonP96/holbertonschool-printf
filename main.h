#ifndef MAIN_H
#define MAIN_H

#include <stdarg.h>
#include <stdlib.h>

int printChar(char);
	int _printf(const char *format, ...);
	int printDouble(float);
	int printString(char *);
	int printInt(int);

#endif
